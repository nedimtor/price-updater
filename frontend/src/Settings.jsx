// frontend/src/pages/Settings.jsx
import React from 'react';
import { Page, Card } from '@shopify/polaris';

const Settings = () => {
  return (
    <Page title="Settings">
      <Card sectioned>
        <p>Settings for sync frequency, billing, and other configurations will go here.</p>
      </Card>
    </Page>
  );
};

export default Settings;
